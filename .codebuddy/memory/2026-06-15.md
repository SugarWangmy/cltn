# 2026-06-15

## dashboard-statistic/6-expense-bookkeeping.html — 经办人改为下拉选择
- 桌面端"记一笔"弹窗中，将经办人字段从卡片式单选（头像+姓名 radio）改为 `<select>` 下拉框
- 新增 `.form-select` CSS 样式（自定义下拉箭头，与 `.form-input` 风格统一）
- 在 `saveExpense()` 中新增经办人必填校验
- 移动端经办人选择（底部弹出面板）保持不变
